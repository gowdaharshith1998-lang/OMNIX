# runtime state — regenerated on analyze, must not be tracked
omnix.db
*.db
*.db-journal
src/web/graph_data.json
src/web/timeline_data.json

# python build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
dist/
build/

# java build artifacts (JavaSemanticEmitter — only the bundled .jar is vendored)
*.class

# node
node_modules/

# editor / OS
.DS_Store
.idea/
.vscode/

# secrets — never commit under any circumstance
.env
.env.local
.env.*.local
*.pem
*.key
secret.pem
public.pem
~/.omnix/

# test artifacts
/tmp/
coverage/
.coverage
htmlcov/

# Cursor Playwright debug artifacts
.playwright-mcp/

# Generated Gephi export (regenerate with scripts/export_gephi.py)
graph.gexf
__pycache__/
*.pyc
# slice 17 cleanup target — orphan SQLite databases at repo root
/omnix.db
/omnix.db-shm
/omnix.db-wal
/0
/0-shm
/0-wal
/1
/1-shm
/1-wal
.omnix/

# Graphify
.venv-graphify/
graphify-out/
GRAPH_REPORT.md
.gitnexus

# AI agent tooling (local, per-developer)
.scratch/
.claude/
.cursor/mcp.json
src/axiom/omnix.db-shm
src/axiom/omnix.db-wal
.claude-flow/

# Local audit manifests (one-off snapshots, not source of truth)
AUDIT_*.md

# Phase 1 hygiene — defensive patterns for fuzz escapes + orphan venvs
.venv-*/
tree_sitter_*/
*.gexf
r10_out.txt
