# OMNIX codebase audit manifest — 2026-05-04

**Scope:** Read-only inspection of `/home/harsh/omnix`. No source edits, no installs, no `omnix analyze` / `omnix studio` execution for this audit.

## Tooling note

- **Ruflo CLI:** `npx ruflo@latest --version` → `ruflo v3.6.27` (evidence: command exit 0).
- **Ruflo hive-mind:** `npx ruflo@latest hive-mind init` completed (**exit 0**, ~100s wall time; `npx` fetched `ruflo@3.6.27` first). Stdout included **“Hive Mind initialized”** with Hive ID **`hive-1777959012798-ijv5nn`** and Queen ID **`queen-1777959012798`** (same shell probe as audit session; **spawn** orchestration was not run).
- **Ruflo MCP agentic-qe** (`ast`, `dependencies`, `complexity`, etc.): **Not invoked** — neural-training and WASM-agent Ruflo tools were skipped per dispatch; static repo tooling used instead (`tree`, `find`, `rg`, `pytest --collect-only`, file reads).
- **Calibra:** Cursor MCP server `user-calibra` descriptors live under the IDE MCP filesystem (`~/.cursor/projects/.../mcps/user-calibra/`); **not** part of the OMNIX git tree.

---

## 1. Repo structure

### 1.1 Directory tree (depth 3, `src/`)

Evidence (`tree -L 3 -d /home/harsh/omnix/src`):

```
/home/harsh/omnix/src
├── agents
├── axiom
├── fabric
│   └── providers
├── find_bugs
├── graph
├── mcp
├── omnix.egg-info
├── parser
│   ├── hints
│   └── quality_profiles
├── providers
│   └── tools
├── scan
│   └── turboscan
├── studio
│   └── frontend
│       ├── dist
│       ├── node_modules
│       └── src
├── verify
│   └── runners
└── web
    └── vault
```

(`tests/`, `docs/`, `benchmarks/`, `scripts/`, `slices/` exist at repo root; not expanded here.)

### 1.2 File counts by extension (project-ish trees, excluding `node_modules` / `__pycache__`)

- **Method:** `find src tests docs slices benchmarks scripts ... -prune node_modules/__pycache__ ...`
- **Total files (that find):** **973**
- **Under `src` + `tests` only (same prune):** **275** `*.py`, **65** `*.tsx`, **63** `*.ts`, plus smaller counts (`json`, `js`, `css`, …).
- **Vitest files:** `find src/studio/frontend/src -name '*.test.ts' -o -name '*.test.tsx'` → **60** files.
- **Pytest files:** `find tests -name '*.py' -type f` → **133** files.

**Note:** Counts include artifacts under repo `.omnix/` test paths when present (e.g. hypothesis constants filenames without extensions).

### 1.3 Top 10 largest files (excluding `.venv`, `.venv-graphify`, `node_modules`, `__pycache__`; includes build artifacts)

| Bytes (approx.) | Path |
|----------------:|------|
| 2,592,965 | `src/web/graph_data_axiom_v2.json` |
| 2,364,761 | `src/studio/frontend/dist/assets/graph_data_axiom_v2-*.js` (built bundle) |
| 1,593,843 | `docs/design-references/chrome_reference.png` |
| 1,498,131 | `docs/design-references/galaxy_reference.png` |
| 1,228,609 | `src/studio/frontend/dist/assets/index-*.js` |
| 567,489 | `tests/axiom/kat_mldsa65.json` |
| 212,538 | `src/studio/frontend/src/components/Graph/viewerEngine.ts` |
| (others) | screenshots under `docs/screenshots/…` |

**If including `node_modules`:** largest entries are Monaco / TypeScript / esbuild bundles under `src/studio/frontend/node_modules/…` (multi‑MB).

### 1.4 Git branches (local + remote)

Evidence (`git branch -a`):

- **Local:** `fix/edge-render`, `main`, `pre-revert-backup`, `rebuild-threejs-attempt`, `slice-14b-bugs-scan`, `slice-15-1-completion`, `slice-15-1-credibility`, `slice-15-2-render-hardening`, `slice-15-3-action-dispatch` (**current `*`**), `slice-15-foundation-fix`
- **Remote:** `origin/main`, `origin/fix/edge-render`, `origin/slice-15-1-completion`, `origin/slice-15-1-credibility`, …

### 1.5 `pyproject.toml` summary

- **Project:** `omnix` **0.1.0**, Python **≥3.10**
- **Deps:** `cryptography`, `click`, `fastapi`, `hypothesis`, `keyring`, `uvicorn[standard]`, `watchdog`
- **Script entry:** `omnix = "cli:main"` → `src/cli.py` (`package-dir` `"" = "src"`, `py-modules = ["cli"]`)
- **Packages listed:** `axiom`, `scan`, `scan.turboscan`, `fabric`, `fabric.providers`, `providers`, `verify`, `find_bugs`, `parser`
- **Pytest:** `pythonpath = ["src"]`, `testpaths = ["tests"]`

### 1.6 `src/studio/frontend/package.json` summary

- **Name:** `omnix-studio` **0.1.0**, **private**, **type module**
- **Scripts:** `dev` (vite), `build`, `typecheck`, `test` / `test:watch` (vitest)
- **Runtime deps:** `@monaco-editor/react`, `d3`, `d3-force`, `gsap`, `monaco-editor`, `pixi.js` **^7.4.3**, `react` 18, `zustand`
- **Dev:** vite, vitest, happy-dom, tailwind, typescript **5.6**

---

## 2. Entry points

### 2.1 CLI surfaces (non-exhaustive; evidence from `pyproject.toml`, `src/cli.py`, `omnix.py`)

| Entry | Location | Notes |
|------|----------|--------|
| **`omnix` (pip)** | `src/cli.py` → `main` | `click` group: `analyze`, `axiom` (`axiom.cli.axiom_group`), `grammar` (`parser.cli.grammar_group`), `find-bugs` |
| **`python omnix.py …`** | `/home/harsh/omnix/omnix.py` | `argparse` subcommands: `analyze`, `studio`, `find-bugs`, `grammar`, `verify`, branches to `axiom.cli` when argv contains `axiom` |
| **`omnix find-bugs`** (pip) | `src/cli.py` `find_bugs_cmd` → `find_bugs.cli.run` | |
| **Grammar** | `parser/cli.py` (`grammar_group`) + `omnix.py` dispatches `grammar` to `src/grammar_cmd.py` (`run_grammar`) | Two wiring paths exist |
| **AXIOM CLI** | `src/axiom/cli.py` (`axiom_group`) | Loaded via pip `omnix axiom` or `omnix.py` axiom branch |
| **Verify CLI** | `src/verify/cli.py` via `omnix.py` `verify` | |
| **MCP server** | `src/mcp/server.py` `main()` | `--project` required, `--db` optional; uses `OmnixTools` |

### 2.2 Studio server entry

- **Function:** `src/studio/server.py` **`run(...)`** lines **1757–1773** — `uvicorn.run(app, host=host, port=p)`; sets `INITIAL_STUDIO_PATH` from `project_path`; default port from `OMNIX_STUDIO_PORT` or **7778** when `port` is `None` (see lines **1765–1772**).
- **FastAPI app:** `app = FastAPI(...)` line **105**.

### 2.3 `sys.path` setup (representative)

| Location | Behavior |
|----------|----------|
| `src/cli.py` lines **17–22** | Inserts **repo root** (`Path(__file__).resolve().parent.parent`) so `from src.X` imports resolve when installed |
| `src/cli.py` `_ensure_repo_root_on_syspath` lines **42–46** | Same for `studio.server` imports |
| `omnix.py` lines **179–182**, **187–188**, **201–203**, **225–226** | Inserts **directory containing `omnix.py`** as root before `src.*` imports |
| `src/mcp/server.py` lines **218–220** | Inserts parent-of-`src` path |

### 2.4 Trace: `omnix analyze /some/repo` (code-level, not executed)

**Pip / `omnix` entry (`src/cli.py`):**

1. `analyze` command lines **63–79**: resolves `path`, calls `_ensure_repo_root_on_syspath()`, imports `src.studio.server.run` as `studio_run`, prints URL, optional browser, **`studio_run(project_path=target, port=port)`**.

**`python omnix.py analyze …` (`omnix.py`):**

1. Lines **221–237**: absolute path, insert repo root on `sys.path`, import `studio_run`, open browser, **`studio_run(project_path=target, port=args.port)`**.

**After server starts:**

2. `run()` stores `INITIAL_STUDIO_PATH` (**server.py** **1769–1770**).
3. Full ingest runs in **`_ingest_block`** → **`ingest_unified_codebase`** (**server.py** **257–264**, import **58**).
4. Workspace lifecycle: **`open_workspace`** referenced **server.py** **1280** (and related routes).

---

## 3. Ingestion / connector architecture

### 3.1 Source interface vs hardwired parser

- **Primary path:** **`ingest_unified_codebase`** in `src/parser/ingest_dispatch.py` — docstring line **1** states single-file graph ingest using **`universal.py`** (“P11: no dedicated-parser edits”).
- **Universal ingest:** `ingest_universal_to_store`, `parse_stats_for_universal_ingest` imported from `universal.py` (**lines 28–29**).
- **Language-specific logic:** Additional parsers exist (`python_parser.py`, `typescript_parser.py`, `dark_matter_parser.py`, `entanglement_parser.py`, `git_parser.py`) — wired through dispatch / optional passes, not a separate “connector plugin registry” discovered in this audit.

### 3.2 Where a non-code source would plug in

- **UNKNOWN —** no dedicated plugin API named in-tree beyond extending **`ingest_unified_codebase`** / path enumeration (**`iter_dispatch_paths`** from `find_bugs.walker`, imported **line 21**) and adding new ingest phases.

### 3.3 Event bus

- **No** in-process `EventBus` / pub-sub matched under `src/**/*.py` (`rg EventBus|event_bus|pubsub` → **no matches**).
- **Studio:** WebSocket + REST push model (`parser_bridge`, `broadcast_to_workspace`); **not** a generic event bus.

---

## 4. Graph schema

### 4.1 SQLite (`GraphStore`)

**File:** `src/graph/store.py` **`_ensure_schema`** lines **53–99**

**Tables:**

- **`nodes`:** `id`, `name`, `type`, `file_path`, `start_line`, `end_line`, `complexity`, `metadata` (JSON text)
- **`edges`:** `id` (auto), `source_id`, `target_id`, `relationship`, `metadata` (JSON), FKs to `nodes`
- **`meta`**, **`file_hashes`** (per-file cache stats)

**Evolution schema (`src/parser/evolution_schema.py`):** `grammar_profile`, `query_pattern`, `pattern_mutation`, `unknown_extensions`, `skip_summary` (+ indexes).

### 4.2 Entity types (node `type` field — string, not enum)

**Observed in code paths:**

- **Universal / exporters:** `file`, `function`, `class`, `method`, `import` (`src/parser/universal.py`, `src/graph/exporter.py` `_TYPE_COLORS` lines **10–17**)
- **Dark matter:** `dark_matter` (`src/parser/dark_matter_parser.py` lines **40**, **63**, **78**)
- **Other:** Doc / studio notes refer to **`directory`**, **`entangled_pair`** in UI parity — backend **ENTANGLED** is primarily an **edge** type between existing node ids (`src/parser/entanglement_parser.py`); node-level **entangled_pair** **UNKNOWN —** verify live WS payloads (not run).

### 4.3 Edge types (`relationship` string)

**From parsers / universal / LLM fallback:**

- `DEFINES`, `IMPORTS`, `CALLS`, `INHERITS`, `DECORATES` (`src/parser/universal.py`, `python_parser.py`, `typescript_parser.py`)
- `ENTANGLED` (`src/parser/entanglement_parser.py` lines **42**, **82**, **252**)
- `DARK_FORCE` (`src/parser/dark_matter_parser.py` lines **156**, **187**, **204**)
- LLM-sourced `IMPORTS` with metadata (`src/parser/llm_fallback.py` line **197**)

### 4.4 Parameterized vs hardcoded

- **Node/edge `type` and `relationship`:** **free TEXT** columns in SQLite — **application-defined strings**.
- **Evolution:** grammar-keyed rows in `grammar_profile` / `query_pattern` — **data-driven** within fixed SQL tables.

### 4.5 Live mutation engine

- **Graph writes:** `GraphStore` methods + transactions; **`reset`**, **`delete_graph_rows_for_file_path`**, **`full_invalidate_ingest_cache`** (`store.py`).
- **Evolution mutations:** `src/parser/evolution.py` (signed receipts noted in `NOTES.md` Integration #11); **`finalize_evolution_run`** called post-ingest **`server.py`** **266–268**.

---

## 5. Calibra

### 5.1 Where it lives (this repo)

- **No** Python package or module named **`calibra`** under `src/`.
- **Name collision:** **`src/scan/turboscan/calibration.py`** — Hypothesis generator calibration for **TURBOSCAN**, not the Calibra product.

### 5.2 What it scores today

- **UNKNOWN —** Calibra scoring logic is **not** in OMNIX source; MCP **`user-calibra`** is external to this repo.

### 5.3 Extraction pipeline shape

- **N/A in-tree** for Calibra. Closest: **TURBOSCAN calibration** + **`ensure_turboscan_calibration`** (`calibration.py`).

### 5.4 API for new fact types

- **UNKNOWN —** not defined in OMNIX.

---

## 6. AXIOM signing (`/home/harsh/omnix` vs `/home/harsh/AXIOM-V2`)

### 6.1 Signing locations in OMNIX (evidence: ripgrep + file reads)

| Concern | Path |
|---------|------|
| ML-DSA-65 (FIPS 204) | `src/axiom/sign.py`, `verify.py`, `params.py`, `ntt.py`, `poly.py`, `encoding.py`, `keystore.py` |
| Merkle | `src/axiom/merkle.py` (`compute_merkle_root`, `merkle_proof` lines **20**, **66**) |
| Ed25519 finding receipts | `src/axiom/finding_keys.py`, `finding_receipt.py` |
| Scan bundles | `src/find_bugs/bundle.py`, `src/find_bugs/receipt_emitter.py` (imports **`compute_merkle_root`** — `receipt_emitter.py` lines **26**, **261**, **286**) |
| Fabric call receipts | `src/fabric/receipts.py` (header comment ML-DSA-65) |
| Scan receipts | `src/scan/receipts.py` |
| Verify receipts | `src/verify/receipt.py` |
| Export narrative | `src/axiom/export_vault.py` (Ed25519 + ML-DSA + Merkle description lines **22–23**) |
| CLI | `src/axiom/cli.py` |

### 6.2 Algorithms

- **ML-DSA-65:** `src/axiom/__init__.py`, `params.py`, `sign.py`
- **Ed25519:** `cryptography.hazmat.primitives.asymmetric.ed25519` in `finding_keys.py`, `provider_vault.py`
- **Merkle:** `src/axiom/merkle.py`

### 6.3 Per-event vs batched

- **Per-finding Ed25519** receipts and **scan manifest** with **Merkle root** + ML-DSA — `src/find_bugs/receipt_emitter.py` header **line 2**; manifest fields include **`merkle_root`** (**lines 286**, **374–376**).
- **Evolution / grammar:** signed JSON + detached `.sig` — `NOTES.md` Integration #11 lines **33–35**.
- **Fabric:** per-call JSON + `.sig` when keys present — `NOTES.md` Integration #3 lines **70–71**.

### 6.4 Receipt schema

- **Multiple JSON envelopes** (finding receipt, scan manifest, evolution receipt, fabric call receipt, verify receipt). Concrete fields: see **`export_vault.py`**, **`receipt_emitter.py`**, **`verify/receipt.py`**.

### 6.5 Policy / runtime governance engine

- **Routing policy:** `src/fabric/policy.py` — `chain_for_task`, `routing_decision`, **`fabric/config.py`** loaded config (`NOTES.md` Integration #3).
- **Budget / health / dedup:** `src/fabric/` modules imported by **`dispatcher.py`** line **17**.
- **Separate policy engine product:** **UNKNOWN —** not identified beyond Fabric routing + budgets.

### 6.6 `/home/harsh/AXIOM-V2`

- **Exists on disk** (`ls`: `apps`, `axiom`, `docs`, `docker-compose.yml`, …).
- **UNKNOWN —** detailed overlap/divergence vs `src/axiom/**` — **not diffed** in this audit.

---

## 7. Visual layer (Studio)

### 7.1 Architecture

- **Backend:** FastAPI **`src/studio/server.py`** — REST + WebSocket + static **`src/studio/frontend/dist`** (**lines 1738–1743**, **1746–1753**).
- **Frontend:** React + Vite under **`src/studio/frontend/src`**; **`Workspace.tsx`** composes **`RightPanel`**, **`GraphCanvas`**, etc. (**imports** show **`RightPanel`** **line 29**).

### 7.2 Renderer

- **`viewerEngine.ts`** imports **`pixi.js`**, **`d3`**, **`gsap`** (lines **3–6**); **legacy engine** “hand-maintained” per header **lines 1–2**.
- **Three.js:** **No** `THREE` import in `viewerEngine.ts` (grep **no hits**). **`NOTES.md`** warns against switching to Three.js mid-bet (**lines 330–332**).
- **WebGPU:** Comment states Pixi v7 uses **`autoDetectRenderer`**; **WebGPU preference requires Pixi v8+** — **`viewerEngine.ts` lines 5571** (comment); test **`slice18a_lite_renderer_detection.test.tsx`** line **26**.

### 7.3 Hex geometry

- **`HEX_BASE_RADIUS`**, **`GLOW_TEXTURE_*`**, galaxy hover radius constants — **`viewerEngine.ts` lines 61–65**, **57–74**.

### 7.4 Drill-down panel

- **Types:** `@/types/drilldown` imported by **`XRayTab.tsx`**, **`GraphCanvas.tsx`**, tests.
- **Right shell:** **`RightPanel.tsx`** — tabs **`xray | code | history | agent:*`** (**lines 8–14**).

### 7.5 WebSocket protocol (server → client)

**Declared types:** `src/studio/ws_protocol.py` **`ALL_SERVER_TYPES`** lines **168–187**:

`bootstrap_start`, `bootstrap_complete`, `node_added`, `edge_added`, `node_modified`, `node_removed`, `edge_removed`, `file_added`, `file_removed`, `stats`, `error`, `pong`, `bugs_scan_started`, `bugs_scan_heartbeat`, `bugs_scan_complete`, `bugs_scan_error`.

**Client → server:** handler responds to JSON **`type: ping`** (**`server.py`** ~**1722–1724** grep hit region).

### 7.6 Timeline scrubber

- **`GET /api/timeline`** returns **`{"snapshots": []}`** — **`server.py` lines 950–952**.
- **`src/studio/NOTES.md` `[debt-21]`** lines **82–86:** compatibility endpoint; **no real git-history source** in live mode yet.

---

## 8. Agent / MCP / skills

### 8.1 OMNIX MCP server

- **Yes** — `src/mcp/server.py`
- **Tools listed** (`tools/list`): **`omnix_search_graph`**, **`omnix_trace_connections`**, **`omnix_get_diagnostics`**, **`omnix_read_file`**, **`omnix_git_blame`** (**lines 36–129**).

### 8.2 Does code emit `SKILL.md` files?

- **No** — **`rg 'SKILL\.md' /home/harsh/omnix/src`** → **no matches** (documentation references at repo root like `AGENTS.md` / `CLAUDE.md` are outside `src/`).

### 8.3 LLM router / Provider Fabric

- **Legacy router:** `src/agents/llm_router.py` — **`LLMRouter`** detects providers via env / localhost Ollama (**lines 11–45**).
- **Fabric:** `src/fabric/dispatcher.py` — validates payload, merges keys, calls providers (**Anthropic**, **openai_compatible**, **google**, **ollama** branches **lines 140–179**); imports **`fabric.policy`**, **`budget`**, **`health`**, **`receipts`**, **`telemetry`** (**line 17**).
- **Registry:** `src/providers/registry.py` — static **`PROVIDERS`** dict (**lines 32+**).

---

## 9. Slice debt registry

### 9.1 Docs read

- **`/home/harsh/omnix/NOTES.md`** — roadmap, integrations, slice **17b** TURBOSCAN, Phase **14b**-* notes.
- **`/home/harsh/omnix/src/studio/NOTES.md`** — **`[debt-13]` … `[debt-32]`** registry-style list.
- **`/home/harsh/omnix/slices/`** contains **`SLICE_18c.md`**, **`SLICE_18d.md`** only (`glob` — **no** `SLICES.md` at repo root).

### 9.2 Requested IDs

| ID | Status | Evidence |
|----|--------|----------|
| **14b** | Present | `NOTES.md` Phase 14b-2 … 14b-4 (~lines **628+**) |
| **debt-18** | Present | `src/studio/NOTES.md` **51–56** |
| **17a–17f, 17i** | Partial | **`17b`** section **`NOTES.md` line 15**; **`17c`** referenced **line 26**; **`17a`,`17d`,`17e`,`17f`,`17i`** — **UNKNOWN —** no standalone tokens in `*.md` grep |
| **18a–18c** | Partial | **`SLICE_18c.md`** exists; **`18a`/`18b` slice docs** — **UNKNOWN —** not found as `SLICE_18a.md` / `SLICE_18b.md` |
| **15.3.7** | Present | Comments **`src/fabric/dispatcher.py` line 115**, **`dispatch_tools.py` line 1**, **`definitions.py` line 1** |
| **D-1..D-7, AB-1..AB-3, R-2, S-1..S-5, BT-2..BT-7** | UNKNOWN — | No unambiguous registry section located via `*.md` grep |
| **debt-29** | UNKNOWN — | No occurrences outside this audit file (`rg debt-29`) |
| **debt-30** | Present | `src/studio/NOTES.md` **133–135** |
| **debt-19.1** | UNKNOWN — | No literal `debt-19.1` in `*.md` grep |

### 9.3 Open debts enumerated in `src/studio/NOTES.md`

**Lines 8–137:** `[debt-13]` resolved; **`[debt-14]`–`[debt-32]`** items listed (subset resolved inline).

---

## 10. Tests

### 10.1 Totals / framework

- **`pytest` collection:** **`442 tests collected, 12 errors during collection`** — errors include **`TypeError: Router.__init__()`** in multiple **`tests/studio/*.py`** files (stderr snapshot during audit).
- **Frameworks:** **pytest** (backend), **vitest** + **happy-dom** (frontend per `package.json`).
- **Frontend test files:** **60** under `src/studio/frontend/src` (`*.test.ts(x)`).

### 10.2 Coverage

- **UNKNOWN —** `pytest --cov` / CI coverage artifact **not run** (would require configured execution).

### 10.3 Slice **15.3.7** — action IDs (from tests)

**File:** `src/studio/frontend/src/lib/actions/registry.test.ts` **lines 6–17** — expected registry keys:

`bugs.explain_finding`, `rightrail.new_agent`, `xray.agent.explain_selection`, `xray.diagnostics.dark_matter.investigate`, `xray.diagnostics.entanglement.explain`, `xray.diagnostics.god_file.split`, `xray.diagnostics.high_complexity.extract`, `xray.diagnostics.high_fan_in.versioned_interfaces`, `xray.diagnostics.high_fan_out.facade`, `xray.diagnostics.orphan_module.investigate`.

---

## 11. Performance

### 11.1 TURBOSCAN timing on AXIOM-V2

- **UNKNOWN —** **not measured** in this audit (no `omnix analyze` / benchmark run per constraints).

### 11.2 Parallel ingest workers

- **`OMNIX_INGEST_WORKERS`** env, default **`max(1, cpu-1)`** capped **`max(..., 12)`** — **`src/parser/ingest_dispatch.py` lines 48–57**.
- **`ProcessPoolExecutor`** / **`ThreadPoolExecutor`** — imports **lines 10–11**, usage region **~144**, **~647–649** (parallel ingest implementation).

### 11.3 Memory caps

- **Tree-sitter LRU:** **`_MAX_ENTRIES = 1000`** — **`src/parser/tree_parse_cache.py` line 12**.
- **Viewer pools:** e.g. **`MAX_POOL_NODES = 200`** — **`viewerEngine.ts` line 50**.

### 11.4 Frontend FPS

- **Sampling:** **`viewerEngine.ts` ~5695–5696** updates FPS via **`setOmnixFpsSample`**
- **Display:** **`BottomToolbar.tsx`** shows **`{Math.round(fps)} FPS`** (**lines 61–68**)
- **Stress overlay thresholds:** **`StressFpsOverlay.tsx` lines 26** — colors at **≥55**, **≥30**, else red
- **Budget:** **UNKNOWN —** no fixed numeric FPS target located beyond overlay thresholds / sampling.

### 11.5 Documented wall-clock expectations

- **`NOTES.md` Slice 17b lines 22–23:** E2E **&lt;30s** “must be confirmed on target hardware” — **not asserted in default CI**.

---

## 12. Naming inventory (code-specific UI strings)

Sample of locations (non-exhaustive; evidence from `rg`):

| File | Line(s) | String / pattern |
|------|---------|------------------|
| `README.md` | 3 | “open-core **code intelligence** product …” |
| `src/cli.py` | 39 | Docstring “**code intelligence** and AXIOM provenance” |
| `src/studio/frontend/src/components/XRayHead.tsx` | 11 | **`X-RAY`** |
| `src/studio/frontend/src/components/XRayMetrics.tsx` | 30–32 | **“Packages (tree)”**, **“Call edges”**, **“Import edges”** |
| `src/studio/frontend/src/store/scopeRegistry.ts` | 19 | **`Repository`** (default label) |
| `src/studio/frontend/src/components/XRayTab.tsx` | 217 | **`Repository`** |
| `src/studio/frontend/src/components/FindBar.tsx` | 19 | **`FIND`** |
| `src/studio/frontend/src/components/LeftRail.tsx` | 138 | Tab **`Bugs`** |
| `src/studio/frontend/src/components/Graph/viewerEngine.ts` | 3132, 3870 | HTML strings **`X-RAY`**, **`X-RAY · SYMBOL`** |
| `src/studio/frontend/src/components/StatsPanel.tsx` | 15–16 | variant **`constellation`** / comment “**constellation** canvas” |
| `src/studio/frontend/src/components/CodeTab.tsx` | 191 | “Select a file or symbol in the **constellation** …” |
| `src/mcp/server.py` | 39–40, 58 | “**code knowledge graph**”, “**code health**”, “**code connections**” |

---

═══ AUDIT COMPLETE — AWAITING DISPATCHER ═══
