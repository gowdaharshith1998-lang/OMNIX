"""PostgreSQL pgoutput logical-replication adapter (PR C D5).

Pure-Python binary parser over the ``pgoutput`` message stream. Replaces
the unmaintained ``pypgoutput`` PyPI prototype (2022, "not production
tested") — same pattern as PR B's pure-Python Datalog evaluator replacing
unmaintained ``pyDatalog``.
"""

from __future__ import annotations

__all__ = []
