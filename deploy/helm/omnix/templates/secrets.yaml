# Secrets are NOT shipped in the chart — they MUST be created out-of-band
# (e.g. via SealedSecrets, External Secrets Operator, or KMS) under the names below.
#
#   omnix-db-dsn            keys: async-url, sync-url
#   omnix-redis-dsn         keys: url
#   omnix-jwt               keys: secret
#   omnix-mldsa-signing     keys: privkey
#   omnix-mldsa-verify      keys: pubkey
#   omnix-drata-api-key     keys: api_key   (only when drata.enabled=true)
#   omnix-sentry-dsn        keys: dsn       (only when sentry.enabled=true)
#
# This file is intentionally empty; it serves as the audit trail.
